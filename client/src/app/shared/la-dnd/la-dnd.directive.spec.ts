import { LaDndDirective } from './la-dnd.directive';

describe('LaDndDirective', () => {
  it('should create an instance', () => {
    const directive = new LaDndDirective();
    expect(directive).toBeTruthy();
  });
});

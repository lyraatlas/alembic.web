api:
  autoredeploy: true
  deployment_strategy: high_availability
  environment:
    - NODE_ENV=development
  image: 'davebrowndev/alembic:latest'
  ports: 
    - "8080:8080"
  restart: always
  target_num_containers: 3
